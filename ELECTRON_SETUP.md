# Electron Desktop Application Setup

This document provides instructions for setting up and building the SATOR Excel PowerPoint Learning Game as a standalone desktop application.

## Prerequisites

- Node.js 18+ and pnpm installed
- Windows OS for building Windows executables

## Required Package.json Changes

Since `package.json` is read-only in the template, you'll need to manually add the following dependencies and scripts:

### Add to `devDependencies`:
